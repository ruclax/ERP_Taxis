import { PlaceholderPage } from '../_components/PlaceholderPage';
import { AlertOctagon } from 'lucide-react';
export default function Page() {
  return <PlaceholderPage title="Bitácora de Accidentes" subtitle="Registro de siniestros vehiculares" icon={<AlertOctagon size={32} />} />;
}
